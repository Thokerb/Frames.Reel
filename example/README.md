# About

These examples are mostly to test the langium engine validation, features, etc. They are semantically correct, but probably not logically doing anything meaningful.
The folders that don't end with _test are actual examples, which also carry some logic. See the thesis for more details.